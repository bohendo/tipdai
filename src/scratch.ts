const { tweet, activateWebhook, getMentions, getSubscriptions, getUser, subscribe, triggerCRC } = require('./twitter')

// getSubscriptions().catch(()=>{})
// subscribe()
// getUser('TipDai')
// activateWebhook()
// triggerCRC()
// getMentions({});
// tweet('Hello World!')
